import styled from 'styled-components';

export default styled.div`
    padding: 10px 15px 10px 15px;
    text-align: left;
    background: white;
    z-index: 999;
    display: flex;
    flex: 0 0 auto;
    flex-direction: row;
`;
